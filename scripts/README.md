# scripts/

One-off and CI orchestration scripts for MBOYO. Nothing here should contain business logic that belongs in `apps/*` or `packages/*` — this directory is for repo-level automation only (e.g., cross-language lint/typecheck orchestration, release helpers).

Populated as needed in later blocks.
