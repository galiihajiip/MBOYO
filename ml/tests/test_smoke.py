"""Smoke test verifying the ml/ package imports cleanly.

A minimal placeholder for `pnpm test` orchestration until real training/eval
tests exist alongside real data (see ml/src/prepare_data.py).
"""

from __future__ import annotations


def test_smoke() -> None:
    assert True
