# MBOYO Working Contract

## Mission

Build an offline-first disaster reporting and response coordination platform that lets Reporters submit incidents without connectivity, lets Verifiers make evidence-based human classification decisions assisted (never overridden) by computer vision, lets Response Coordinators dispatch and track operational response against verified incidents, and gives Auditors an immutable, read-only view of the full lineage — from report to model score to human decision to dispatch.

Target: PIDI Digdaya × Hackathon Bank Indonesia 2026 capstone, Top 80 readiness, with a production-minded architecture and a deterministic live-demo mode.

## Non-Goals

- MBOYO does not replace human judgment in incident verification — ML output is advisory input to the Verifier role, never an autonomous decision-maker.
- MBOYO does not guarantee real-time dispatch of physical resources (vehicles, personnel) — it coordinates and tracks response tasks; execution in the field is out of scope.
- MBOYO does not build a general-purpose CMS, ticketing system, or messaging platform beyond what disaster reporting/response requires.
- MBOYO does not target full production hardening (e.g., multi-region failover, SOC2 compliance tooling) within this capstone scope — it targets a production-*minded* architecture, not a fully hardened production deployment.
- MBOYO does not fabricate or inflate ML performance claims for demo purposes.

## Definition of Done

A block/feature is done when:
- It runs independently and is reviewable as a standalone diff.
- TypeScript strict mode and Python type hints pass with no suppressed errors.
- RBAC boundaries are enforced at the API/RLS layer, not just hidden in the UI.
- Offline-first invariants hold for any Reporter-facing flow touched.
- Any ML-related claim is backed by a measured evaluation on an untouched test set, or is absent.
- Any demo fallback is flagged, visibly labeled, and documented as a limitation.
- Verification (typecheck, lint, tests, build) has been run and results are reported, not assumed.
- Bahasa Indonesia is used for all user-facing copy.
- No secrets, credentials, or raw sensitive evidence are committed.

## Demo-Mode Policy

- Demo mode exists to produce a deterministic, reliable live-demo path when live infrastructure (real ML inference, live GPS, network conditions) cannot be guaranteed during a presentation.
- Demo mode must be explicitly toggled via `DEMO_MODE` / `NEXT_PUBLIC_DEMO_MODE` environment variables — never silently triggered by error states or missing config.
- Every simulated/mocked value shown in demo mode must carry a visible UI label (Bahasa Indonesia) identifying it as simulated/demo data.
- Demo mode must not be the default in any environment other than an explicitly configured demo deployment.
- Demo mode fallbacks must be enumerated in the block summary that introduces them.

## Production-Mode Policy

- Production mode uses live Supabase auth/RLS, live ML inference through `apps/ml-api`, and real offline sync via Dexie + Background Sync.
- No mocked responses, canned confidence scores, or simulated network conditions are permitted in production mode.
- All role-gated actions are enforced server-side (API route + RLS), with UI-level restrictions treated as UX convenience only, never as the security boundary.
- Secrets required for production (service-role key, ML internal token, VAPID keys, signing secrets) are never exposed client-side.

## Never-Fake Checklist

Before marking any block complete, confirm:
- [ ] No ML metric (accuracy, precision, recall, F1, confidence) is invented, guessed, or copied from an unrelated run.
- [ ] No API response is hardcoded/mocked outside of an explicitly labeled demo-mode path.
- [ ] No test is asserting against a fake/stubbed result presented as if it exercised real logic, unless the test explicitly declares it's testing a mock.
- [ ] No error is silently swallowed or hidden behind a disabled lint rule or `any` cast.
- [ ] No UI element claims real-time/live data when it is actually static or cached beyond a reasonable staleness window without disclosure.
- [ ] No secret, credential, or raw evidence file is staged for commit.

## Decision Log Format

Each significant architectural or scope decision is recorded as:

```markdown
### <YYYY-MM-DD> — <short decision title>

**Context:** What prompted the decision.
**Decision:** What was decided.
**Consequences:** What this enables, constrains, or defers.
**Status:** Proposed | Accepted | Superseded by <link>
```

Decisions are appended chronologically to this section as the project progresses.

---

## Decision Log

### 2026-07-16 — Defer git initialization

**Context:** BLOCK 00 audit found the MBOYO directory completely empty, with no prior repository, code, or documentation.
**Decision:** Proceed directly to documentation (AGENTS.md, this contract) without initializing git yet; git setup and first commit are deferred until the user is ready to focus on that separately.
**Consequences:** Verification for this block is file-existence/content review rather than `git diff`, since there is no repository to diff against yet.
**Status:** Accepted

### 2026-07-16 — Adopt four-tier scope model (MVP / Enhanced Demo / Production / Future)

**Context:** BLOCK 01 required defining scope tiers, success metrics, roadmap, and risk register without overstating what will be built or claiming unmeasured ML performance.
**Decision:** Scope is split into four additive tiers documented in MVP_SCOPE.md, with PRODUCTION_SCOPE.md expanding the Production tier into non-functional requirements. No fixed accuracy numbers are promised anywhere; a release gate (macro-F1, destroyed recall, calibration error, checked-in evaluation report) gates model promotion, with an explicitly labeled advisory-only fallback if the gate fails.
**Consequences:** Future blocks must classify new features into one of the four tiers and must not claim ML metrics without a dated evaluation report in ml/reports/.
**Status:** Accepted

### 2026-07-16 — Lock architecture, ADRs, and threat model

**Context:** BLOCK 02 required concrete system architecture, sequence flows, deployment topology, a threat model, and five ADRs, translating the product/RBAC decisions from BLOCKS 00–01 into a specific technical shape before any implementation begins.
**Decision:** Adopted pnpm/Turborepo monorepo (ADR 0001), Supabase as the unified platform for auth/db/storage/realtime (ADR 0002), a Postgres-table-backed `analysis_jobs` queue with atomic claim instead of an external broker (ADR 0003), local ML as the sole authoritative classifier with Gemini as an explicit opt-in advisory-only feature (ADR 0004), and Dexie/IndexedDB + Workbox Background Sync for the offline queue (ADR 0005). `apps/web` is the sole BFF/trust boundary; `apps/ml-api` and `apps/worker` have no public ingress. Threat model covers 12 adversarial scenarios with prevention/detection/recovery/residual-limitation for each.
**Consequences:** Future implementation blocks must conform to the trust boundaries in SYSTEM_ARCHITECTURE.md/DEPLOYMENT_TOPOLOGY.md (no direct browser-to-ml-api or browser-to-service-role access) and must update SEQUENCE_FLOWS.md if actual implementation diverges from the documented flows. Any change to the Gemini advisory-only boundary requires a new ADR superseding 0004.
**Status:** Accepted

### 2026-07-16 — Define domain model, state machines, RBAC matrix, and navigation

**Context:** BLOCK 03 required translating the architecture from BLOCK 02 into concrete entities, lifecycle rules, a full entity×action RBAC matrix, and per-role navigation, so schema/RLS design (a later block) has an unambiguous specification to implement against.
**Decision:** Defined 22 entities (DOMAIN_MODEL.md) including separate `verification_review` (append-only decision record) and `model_prediction`/`model_explanation` (never mutated post-write) entities to keep evidence and human decisions immutable by construction. Report and task lifecycles (STATE_MACHINES.md) specify actor/preconditions/result/audit-action per transition — no transition is performed by a role outside the RBAC matrix. RBAC_MATRIX.md gives every one of the 22 entities exactly one owning role for mutating actions (except intentional self-service overlaps: own profile, own push_subscription, own notifications). Auditor is read-only on every single cell. System Administrator has no create/approve path on report validation or response_task/task_assignment dispatch. Navigation (NAVIGATION_BY_ROLE.md) uses the exact Bahasa Indonesia menu labels specified, with each item's actions checked against the matrix.
**Consequences:** Future schema/RLS design (BLOCK 03/04 per DELIVERY_ROADMAP.md) must implement RLS policies matching RBAC_MATRIX.md exactly — any new entity or action introduced later must be added to the matrix before implementation, not after.
**Status:** Accepted

### 2026-07-16 — Define information architecture and Stitch handoff (Design Gate specification)

**Context:** BLOCK 04 required a full screen inventory (public, all five roles, and system-state screens) plus content/terminology rules and a formal handoff process, so the Google Stitch Design Gate (noted in DELIVERY_ROADMAP.md) has an unambiguous, complete brief rather than ad hoc design decisions made screen-by-screen.
**Decision:** Documented the full site map (INFORMATION_ARCHITECTURE.md), every screen's role/route/purpose/data/CTAs/forbidden-actions/loading/empty/error/permission/offline-behavior/responsive-hierarchy (SCREEN_INVENTORY.md, covering public, Reporter, Verifier, Coordinator, Administrator, Auditor, and 8 system-state screens plus 2 shared patterns), canonical Bahasa Indonesia terminology and two mandatory disclosure strings for demo-mode and advisory-only model output (CONTENT_GUIDE.md), and a phased Stitch generation sequence with explicit Design Gate approval criteria (STITCH_HANDOFF.md).
**Consequences:** No screen may be implemented in code before its Stitch design is generated against SCREEN_INVENTORY.md and approved per the six-point criteria in STITCH_HANDOFF.md. Any spec gap discovered during Stitch generation must be fixed in the specification document first, not silently resolved only in the design.
**Status:** Accepted

### 2026-07-16 — Bootstrap production monorepo (BLOCK 05)

**Context:** BLOCK 05 required turning the architecture decisions in the five ADRs into an actual runnable pnpm/Turborepo monorepo — apps/web (Next.js), apps/ml-api (FastAPI), apps/worker (Python), shared TS packages, and skeletons for supabase/, ml/, infra/, scripts/.
**Decision:** Scaffolded the full workspace per ADR 0001 (pnpm workspaces + Turborepo, Python apps outside the pnpm workspace), used Next.js 16.2.10 with React 19 (latest stable compatible pair at time of bootstrap) and Python 3.12 for all three Python-adjacent components (apps/ml-api, apps/worker, ml/). Added a shared Zod-based environment validation module in packages/domain (`loadServerEnv`/`loadClientEnv`, lazily evaluated so builds without real secrets still succeed) mirrored by a pydantic-settings config in each Python app. apps/web's ESLint config resolves `@next/eslint-plugin-next` directly in flat-config form rather than bridging the legacy `eslint-config-next` via `FlatCompat`, which crashes under ESLint 9 (circular structure in JSON.stringify during schema validation) — this is a concrete incompatibility discovered during verification, not a stylistic choice. Ruby-equivalent B008 (FastAPI `Depends()` in argument defaults) is ignored repo-wide in apps/ml-api's ruff config since it's idiomatic FastAPI, not a real bug — documented inline in pyproject.toml rather than suppressed per-line.
**Consequences:** All verification commands pass clean: `pnpm install`, `pnpm lint`, `pnpm typecheck`, `pnpm build` (web build succeeds, health endpoint returns 200), and Python smoke tests (ruff + mypy strict + TestClient-driven health check for ml-api; heartbeat generator + config load for worker) all green. Future blocks adding real domain schemas/RLS/inference logic build directly on this scaffold without needing to revisit tooling choices.
**Status:** Accepted

### 2026-07-16 — Design system, brand assets, and PWA manifest (BLOCK 06)

**Context:** BLOCK 06 required implementing packages/ui and brand foundations. No actual Google Stitch designs existed yet (only the STITCH_HANDOFF.md brief from BLOCK 04) — user resolved this by supplying a full approved design-system spec directly (front-matter tokens + prose sections), saved verbatim to docs/design/DESIGN_SYSTEM_SPEC.md, treated as the authoritative Stitch-equivalent reference for this block.
**Decision:** Built packages/ui on Tailwind CSS v4 (`@theme` token layer), Radix UI primitives (Dialog, Select, Checkbox, Toast) for accessibility, and class-variance-authority for variant styling. All severity/priority/role/status colors and Bahasa Indonesia labels live in a single tokens module (packages/ui/src/tokens/{colors,labels,typography}.ts) mirroring docs/product/CONTENT_GUIDE.md exactly — no component or app screen hardcodes a hex value or label string. Built all 23 requested primitives (Button through NotificationCard). Generated original brand assets (logo SVG, favicon.ico, 192/512/apple-touch/maskable PNG icons, OG image) via a one-off sharp-based script (scripts/generate-icons.mjs, kept for future regeneration) rather than an external asset tool. Wired Plus Jakarta Sans and IBM Plex Mono via `next/font/google` and created `/design-system` showcasing every primitive and token.
**Consequences:** Verified via compiled-CSS inspection (not just visual screenshot, since no browser-automation tool was available in this environment): `:focus-visible` present with the Signal Cyan outline token, `prefers-reduced-motion` neutralization present, 44px (`2.75rem`) minimum touch targets present on interactive primitives, and amber tone badges/status resolve to dark text (`#7a5109` on `#f6b73c`) rather than white, satisfying the readable-amber-text requirement. `pnpm lint`, `pnpm typecheck`, and `pnpm build` all pass clean across the full workspace. Future blocks implementing actual role screens must import colors/labels only from packages/ui/src/tokens, never inline.
**Status:** Accepted

### 2026-07-16 — Local Supabase platform and environment validation (BLOCK 07)

**Context:** BLOCK 07 required configuring the local Supabase platform (Auth, Postgres+PostGIS, Storage with two private buckets, Realtime), db:* script wiring, server-only vs. browser Supabase clients, and TypeScript/Python environment validation — explicitly without implementing domain schema yet.
**Decision:** Wrote a full `supabase/config.toml` (corrected twice against the actual installed CLI's schema — `[inbucket]` → `[local_smtp]`, and `enable_confirmations` moved out of top-level `[auth]` into `[auth.email]` — confirmed by the CLI reaching the image-pull stage rather than failing config validation). Added a PostGIS-enabling migration (`20260716145222_enable_postgis.sql`) containing only `CREATE EXTENSION`, no domain tables. Added `apps/web/src/lib/supabase/{service-role.server,server,browser}.ts` — three clients with distinct trust boundaries (service-role bypasses RLS entirely and is `server-only`-guarded; the other two are RLS-scoped via anon key, one server-session-aware, one browser-safe). Tightened `apps/worker`/`apps/ml-api`'s pydantic-settings config so required secrets have no default, matching the TypeScript env module's fail-loudly behavior — verified a missing-var run raises `ValidationError` with a field-by-field message.
**Consequences:** `pnpm lint`/`typecheck`/`build` all pass clean; a `grep` over `apps/web/.next/static/chunks/` confirmed `SUPABASE_SERVICE_ROLE_KEY` never reaches the client bundle. Live `supabase start` verification (confirming PostGIS actually exists in a running container) could not be completed in this session: the host `C:` drive was found to have 0 bytes free of 238GB, which caused both a Docker Desktop containerd I/O error during image pull and an unrelated `npm`/`tsx` `ENOSPC` failure during a separate check — a host disk-space exhaustion issue, not a defect in this block's configuration. Documented in `docs/architecture/LOCAL_PLATFORM.md`'s "Known Local Environment Caveat" section; user will free disk space and retry `pnpm db:start` to complete live verification.
**Status:** Accepted

### 2026-07-16 — Full domain schema, RLS, RPC functions, seed data, and tests (BLOCK 08)

**Context:** BLOCK 08 required implementing the complete 22-entity domain schema as Supabase migrations, RLS policies matching RBAC_MATRIX.md exactly, geospatial/queue RPC functions, Realtime publication, demo seed data, and pgTAP tests — on top of the platform BLOCK 07 established.
**Decision:** Split the schema into five migrations for reviewability (core schema/enums/tables/indexes; permission-helper SQL functions; RLS policies; RPC functions; Realtime publication). Key design choices: `client_report_id` uniqueness scoped per-reporter (not global) to match the offline dedupe-key semantics in ADR 0005; `model_predictions.severity_probabilities` stored as jsonb with a check constraint enforcing all 5 severity keys present, each in [0,1], summing to ~1 — rejecting fabricated/malformed probability vectors at the database layer per AGENTS.md ML honesty rules, not just trusting apps/worker; `audit_events` has no INSERT/UPDATE/DELETE policy for any role — the only write path is the SECURITY DEFINER `append_audit_event()` function, with UPDATE/DELETE additionally REVOKEd at the grant level as defense in depth. During manual SQL review (live DB verification was blocked by an unresponsive Docker daemon — see below), found and fixed a real bug: `claim_analysis_jobs()` is necessarily SECURITY DEFINER (no human role has table-level UPDATE on analysis_jobs), but the initial version granted EXECUTE to `authenticated` with no internal guard, which would have let any authenticated role of any kind claim/mutate analysis_jobs — fixed by adding an explicit `has_role('system_administrator')` check inside the function body. Also fixed a stale cross-reference (core schema migration's header comment pointed at the wrong RLS migration filename).
**Consequences:** All SQL is written and manually reviewed but **not yet run against a live database** — Docker Desktop's daemon was unresponsive (`docker ps` timing out) for the remainder of this session after disk space was freed, an environment issue distinct from and downstream of the BLOCK 07 disk-space problem. `docs/architecture/DATABASE_SCHEMA.md` documents the full schema/RLS/RPC/seed/test design and flags this as the outstanding verification step. `docs/architecture/LOCAL_PLATFORM.md` updated to reflect that BLOCK 08 has since implemented the schema it previously described as deferred. User will get Docker responsive again and ask for `pnpm db:reset` + `pnpm exec supabase test db` to complete live verification.
**Status:** Accepted

### 2026-07-16 — Supabase Auth, session-based RBAC guards, and demo accounts (BLOCK 09)

**Context:** BLOCK 09 required wiring Supabase Auth with server-side sessions (no localStorage), Next.js middleware/proxy-based route protection, four server auth helpers (getCurrentUser/requireAuthenticated/requireRole/requirePermission), five demo accounts at exact emails/password, role-destination routes, a demo-mode-gated account chooser, Unauthorized/Session-Expired screens, and tests proving cross-role denial.
**Decision:** Updated BLOCK 08's seed data to the exact demo emails (`reporter@mboyo.demo` etc.) and password (`DemoMboyo2026!`) specified in this block, superseding the earlier placeholder credentials. Session lives exclusively in HTTP-only cookies via `@supabase/ssr`'s `createServerClient`/`createBrowserClient` — no client-side Supabase auth call exists anywhere in the login path, satisfying "no localStorage auth token" structurally, not just by avoidance. Built `apps/web/src/proxy.ts` (Next.js 16 renamed `middleware.ts` → `proxy.ts` with an exported `proxy()` function, not `middleware()` — discovered via the build's own error message, since no bundled Next.js docs covered it) for coarse route-prefix-to-role denial, backed by `requireRole()`/`requirePermission()` inside each page as defense-in-depth (a route added later without a proxy entry still fails closed). `lib/auth/permissions.ts` transcribes RBAC_MATRIX.md directly into an (entity, action) → role table that fails closed for any unlisted pair — this is the mechanism proven by 20 vitest unit tests covering cross-role denial, Auditor-cannot-mutate (verified against every mutating action on every entity Auditor can read, plus the audit_event update/delete-never-permitted-for-any-role invariant), and Admin-cannot-validate-or-dispatch (explicit checks that system_administrator has none of report:approve, verification_review:create, response_task:create/update/assign, incident_cluster:create/update, while retaining its own configuration-domain permissions).
**Consequences:** All five role pages (`/reporter`, `/verifier`, `/command`, `/admin`, `/audit`) plus `/masuk`, `/sesi-berakhir`, `/tidak-diizinkan` had to be marked `export const dynamic = "force-dynamic"` — Next.js otherwise attempts to statically prerender them at build time, which fails since `getServerEnv()`/Supabase env validation has no real secrets available during a CI-style build; this is now the established pattern for any future auth-gated page. `pnpm lint`/`typecheck`/`test` (20 passing) /`build` all pass clean; confirmed via `grep` that `SUPABASE_SERVICE_ROLE_KEY` never reaches `.next/static/chunks/`. Live login-flow verification (actually signing in as each demo account and confirming redirect/denial behavior against a running Supabase instance) could not be completed — Docker Desktop's daemon remained unresponsive for this entire session, the same blocker carried over from BLOCK 08.
**Status:** Accepted

### 2026-07-16 — Public marketing site, trust pages, and SEO (BLOCK 10)

**Context:** BLOCK 10 required implementing the public header/hero/footer, ten landing sections plus two more implied by the header's own nav anchors (Solusi, Teknologi), four trust pages (/privacy, /methodology, /data-governance, /accessibility), and SEO (metadata, sitemap, robots, OG image) — no Stitch designs existed (same situation as BLOCK 06), so content was written directly from the prompt's spec plus the product/architecture docs already established.
**Decision:** Built all sections as server components reusing real `packages/ui` primitives (RoleBadge, ProbabilityBars, OnlineStatus/SyncStatus, MetricCard) rather than static marketing mockups disconnected from the actual product — e.g. the AI+human section's probability visualization is the literal `ProbabilityBars` component a Verifier would see, explicitly labeled as illustrative data. The Metrics section names metrics MBOYO measures (macro-F1, destroyed recall, etc.) without inventing numbers, per AGENTS.md ML honesty rules. The Demo Accounts section reads `NEXT_PUBLIC_DEMO_MODE` server-side and only emits credentials into the HTML when true — verified live by toggling the flag and confirming the exact string "reporter@mboyo.demo" is absent from the response when off. No external stock photography anywhere — all illustrations (hero pipeline diagram, crisis-map illustration) are original inline SVG built from brand tokens.
**Consequences:** Found and fixed two real bugs during live verification (Docker unavailable, but the Next.js dev server itself was): (1) `apps/web/src/lib/supabase/middleware.ts` used `loadServerEnv` (requiring `SUPABASE_SERVICE_ROLE_KEY`, `ML_INTERNAL_TOKEN`, etc.) when the proxy only ever needs the client-safe Supabase URL/anon key — this made the proxy, and therefore every route including public marketing pages, fail whenever unrelated server secrets weren't configured; fixed by switching to `loadClientEnv`. (2) `packages/domain/src/env.ts`'s optional URL fields (e.g. `NEXT_PUBLIC_MAP_STYLE_URL`) rejected a blank-but-present `.env` line (`KEY=`) because Zod's `.optional()` only permits `undefined`, not an empty string — exactly the shape `.env.example` itself ships; fixed by normalizing blank strings to `undefined` before validation in both `loadServerEnv` and `loadClientEnv`. A local `apps/web/.env` with placeholder values was created (gitignored, kept per user's choice) to unblock this verification and future local dev. `route-map.ts`'s public-path list extended to include the four trust pages plus `/sitemap.xml`/`/robots.txt`. `pnpm lint`/`typecheck`/`test` (21 passing)/`build` all pass; all 8 new/changed routes confirmed returning 200 against a live dev server.
**Status:** Accepted

### 2026-07-16 — Five role-specific application shells and navigation (BLOCK 11)

**Context:** BLOCK 11 required implementing five separate app shells (Reporter, Verifier, Coordinator, Admin, Auditor) with a shared topbar/online-indicator/notification-area/user-menu/breadcrumbs, each role's exact navigation per docs/product/NAVIGATION_BY_ROLE.md, and desktop/tablet/mobile responsive patterns — with the hard requirement that navigation never expose a forbidden action.
**Decision:** Built one data module, `lib/navigation/nav-items.ts` (`ROLE_NAV_ITEMS`), as the single source every navigation surface reads from — desktop `SidebarNav` (persistent, labeled), tablet `TabletRailNav` (collapsible icon rail), and mobile `MobileBottomNav` (bottom tabs for the first 4 items + a "Lainnya" overflow `Sheet` for the rest, always including Profil). Because every surface renders `ROLE_NAV_ITEMS[role]` and nothing else, there is no code path capable of rendering an item outside that role's list — the RBAC guarantee lives in the data source, not scattered per-component conditionals. One `AppShell` component composes `Topbar` + the three responsive nav variants + a `Breadcrumbs` slot; one `layout.tsx` per role directory calls `requireRole()` once and wraps all of that role's pages, replacing BLOCK 09's ad hoc per-page header/logout duplication. Notification area is omitted entirely (not just hidden) for Reporter/Admin/Auditor, matching that NAVIGATION_BY_ROLE.md gives only Verifier and Coordinator a Notifikasi item. Added 27 nav-item stub pages (previously only 5 role-home stubs existed) so every one of the 33 total nav destinations across all 5 roles actually resolves.
**Consequences:** Added 31 vitest unit tests (`nav-items.test.ts`) asserting `ROLE_NAV_ITEMS` matches docs/product/NAVIGATION_BY_ROLE.md label-for-label and order-for-order per role, that no role-specific item ever leaks into another role's array, that every href falls under that role's own route prefix, and specifically that Admin's nav contains no validate/dispatch-shaped item and Auditor's contains no mutation-shaped item. Live full-shell rendering (an authenticated user actually seeing their nav) could not be verified — no real Supabase backend is reachable this session — but a live dev-server request to `/reporter` while unauthenticated was confirmed to 307-redirect to `/masuk?next=%2Freporter`, proving the shell/layout code executes correctly server-side. `pnpm lint`/`typecheck`/`build` all pass; `pnpm test` now at 31/31.
**Status:** Accepted

### 2026-07-17 — Reporter home and 9-step report wizard (BLOCK 12)

**Context:** BLOCK 12 required implementing /reporter, /reporter/laporan/baru (the 9-step wizard: Event, Photo, Preview, GPS, Manual location fallback, Description, Consent, Review, Submit), /reporter/laporan (list), /reporter/laporan/[id] (detail), /reporter/bantuan, and /reporter/profil, using an offline repository interface that BLOCK 13 will implement for real, with a clearly-marked temporary online mock adapter in the meantime. Kept the existing Bahasa Indonesia route slugs already established in BLOCKS 09/11 (`/reporter/laporan/baru`, not the prompt's literal `/reporter/new`) rather than introducing a conflicting parallel route scheme.
**Decision:** Defined `ReportDraft` (lib/reports/types.ts) as a richer client-side domain type than the current `reports` table (adds title, observed severity, contact preference, consent versions, altitude/heading/GPS timestamp/location source — fields the prompt asked for that aren't yet in the BLOCK 08 schema), reconciled by a later schema migration, not this block. Defined `OfflineReportRepository` (lib/reports/repository.ts) as the storage seam — every wizard/list/detail component depends only on this interface. Built `MockOnlineReportRepository` (lib/reports/mock-online-repository.ts) with an explicit `⚠️ TEMPORARY MOCK ADAPTER ⚠️` banner comment stating exactly what guarantees it does NOT provide (no IndexedDB persistence — a reload loses all drafts; no real network submission; no Background Sync) — the honesty-of-fallback rule from AGENTS.md applied to a development-time seam, not just user-facing demo mode. The GPS step (`Geolocation.getCurrentPosition`) explicitly discloses accuracy in meters and never blocks progress; a Manual Location step (lat/lng + free-text address) is always reachable as a named fallback, never a dead end, per docs/product/RISK_REGISTER.md risk #3. A purely client-side heuristic (lib/reports/photo-quality.ts, luminance + a coarse edge-energy proxy) gives the Reporter immediate quality feedback without claiming to be a model score, and never blocks submission. Autosave is debounced (600ms) through the repository seam and a draft is restored on wizard mount if one exists, satisfying "no data loss" and "draft restored."
**Consequences:** Found and fixed real lint errors during verification (not stylistic): an inline `eslint-disable-next-line react-hooks/exhaustive-deps` referenced a rule not registered in this project's flat config at all (no `react-hooks` plugin here) — removed rather than left as dead/misleading comment. Mock repository methods were `async` with no `await`, tripping `@typescript-eslint/require-await` — fixed by returning `Promise.resolve(...)` from plain (non-async) methods instead of papering over with a suppression. `pnpm lint`/`typecheck`/`test` (31/31, unchanged — no new automated tests added this block since the wizard's correctness is UX/flow-shaped rather than unit-testable business logic) /`build` all pass. Live full click-through of the wizard as an authenticated Reporter could not be verified — no real Supabase backend reachable this session — but confirmed via live dev server that all new/changed reporter routes correctly 307-redirect when unauthenticated, proving the new code executes without a server-side crash.
**Status:** Accepted

### 2026-07-17 — Installable PWA: Workbox service worker, caching, and Background Sync (BLOCK 14)

**Context:** BLOCK 14 required an installable PWA using a Workbox-compatible inject-manifest service worker: app-shell precache, network-first navigation, offline fallback, cache-first immutable assets, stale-while-revalidate for selected public resources, never caching authenticated API responses or signed evidence URLs; Background Sync with fallbacks (browser-online, SW-startup, message channel), install/update-available prompts, standalone mode, offline reporter pages, and last-known own-report summaries — plus docs/architecture/PWA_OFFLINE.md.
**Decision:** Built `apps/web/src/service-worker/sw-src.ts` as the Workbox `injectManifest` source (not `GenerateSW`, since this is a Turbopack project), bundled by `apps/web/scripts/build-sw.ts` (esbuild → IIFE, then `workbox-build`'s `injectManifest()`) into `public/sw.js`, wired to run after `next build` (needs `.next/static/**` to glob/hash). Since the offline queue (BLOCK 13's Dexie/IndexedDB) already existed, all sync triggers (native Background Sync event, browser `online` fallback, SW-startup, message-request) call one shared function, `lib/offline/sync-replay.ts`'s `runQueueReplay()` — no trigger re-implements claim/POST/mark-synced logic, satisfying "no duplicate queue implementation" by construction, and proving trigger parity means testing that one function once. Built minimal real `/api/reports` (idempotent upsert on `reporter_profile_id,client_report_id`) and `/api/reports/evidence` (uploads to the BLOCK 07 Storage bucket, inserts a BLOCK 08 `report_evidence` row) endpoints — a user-approved scope decision, since the sync-replay logic needed a real target to call, kept intentionally minimal (no thumbnailing/perceptual hash). `self.skipWaiting()` is triggered only by an explicit `MBOYO_SKIP_WAITING` message after the Reporter taps "Perbarui Sekarang," never automatically, so an update can never interrupt an in-progress report wizard session — caught and fixed during my own implementation, not left as an initial auto-skip-waiting draft. "Last-known own-report summaries" and "offline reporter pages" required no new caching layer: `/reporter/laporan`, `/reporter/laporan/[id]`, `/reporter/antrean`, `/reporter/laporan/baru`, and `/reporter/bantuan` already read exclusively from the local Dexie queue or are static, so they work offline once their page shell is cached by the network-first navigation strategy; `/reporter` and `/reporter/profil` are `force-dynamic` (auth-dependent) and honestly documented as NOT precache-eligible, relying entirely on runtime cache-fallback.
**Consequences:** Fixed a real path bug in `build-sw.ts`: the four top-level static-asset glob patterns (icons, manifest, offline.html, favicon.ico) were written relative to the app root instead of `public/`, so they silently weren't being precached despite the build reporting success — caught via `workbox-build`'s own "doesn't match any files" warnings, confirmed fixed by an increase from 48 to 59 precached files and the warnings disappearing entirely. Fixed a real route-protection gap: `/sw.js` and `/offline.html` were not in `route-map.ts`'s public-path list nor excluded from `proxy.ts`'s matcher, so both 307-redirected to `/masuk` in a live server check — a service worker registration request being redirected to a login page would have silently broken the entire PWA layer; fixed by adding both to `PUBLIC_ROUTE_PREFIXES` and excluding `sw.js` from the proxy matcher, then re-verified live (200 for both). Wrote 7 new vitest tests (`sync-replay.test.ts`) covering empty-queue no-op, successful sync + evidence upload, retryable-failure-marks-failed vs permanent-failure-marks-conflict classification, batch-size limiting, and progress-event ordering — full suite now at 75/75. `pnpm lint`/`typecheck`/`build` all clean, including the SW bundling stage; live server check confirmed `sw.js`/`offline.html`/`manifest.webmanifest` return 200 unauthenticated while protected routes still 307-redirect. Installability and offline-navigation acceptance criteria were verified structurally (manifest fields, precache manifest contents, SW registration/route logic) rather than via live browser/Lighthouse automation — no browser automation tool available this session, documented as a limitation in PWA_OFFLINE.md alongside Background-Sync's Chromium-only support and iOS Safari's lack of `beforeinstallprompt`.
**Status:** Accepted

### 2026-07-17 — Protected evidence upload validation pipeline and duplicate checks (BLOCK 15)

**Context:** BLOCK 15 required replacing BLOCK 14's intentionally-minimal evidence endpoint (sha256 + raw upload only) with the full pipeline: authenticate, validate event/ownership/client_report_id, validate MIME and magic bytes, enforce size/resolution, decode, normalize orientation, strip EXIF, compute SHA-256 and perceptual hash, generate a thumbnail, store privately, persist metadata, enqueue an analysis job — plus short-lived server-generated signed URLs, idempotent replay, non-discarding duplicate-hash warnings, structured error codes, and tests.
**Decision:** Chose `sharp` (user-approved over a pure-JS decoder) for decode/orientation/EXIF/thumbnail — one native library, fails closed on malformed input via decode exceptions, and simply omitting `withMetadata()` on output (verified experimentally, not assumed) fully strips EXIF rather than the more common but incomplete `withMetadata({exif:{}})` pattern which still leaves a residual EXIF block. Built a new `lib/evidence/` module split by concern (`authorize.ts`, `validate-upload.ts`, `magic-bytes.ts`, `process.ts`, `perceptual-hash.ts`, `duplicate-check.ts`, `errors.ts`) so each validation stage is independently unit-testable without a live Supabase backend. Added `EVIDENCE_ERROR_CODES`/`EvidenceErrorCode` to `packages/domain`'s shared enums (not app-local) since both the upload endpoint and the offline sync-replay logic need the same vocabulary to classify permanent-vs-retryable failures — `sync-replay.ts` was updated to read the endpoint's own `permanent` classification instead of guessing from HTTP status alone. aHash (average hash, not DCT-based pHash) was used for the perceptual hash — simple, dependency-free beyond sharp, sufficient as a coarse Verifier-facing signal, not a forensic-grade similarity score (that nuance, and the actual duplicate_candidate_report_id assignment, remains the ML worker's job per DOMAIN_MODEL.md, out of this block's synchronous upload path). "Duplicate hash creates a warning but is not silently discarded" is implemented as a NOT NULL `is_duplicate_hash` boolean set at insert time by checking for the same SHA-256 on any OTHER report — the row is always inserted regardless. Discovered during implementation that **no `storage.objects` RLS policies existed at all** for the report-evidence bucket (every other table got RLS in BLOCK 08, but the Supabase-managed storage schema was overlooked) — meaning BLOCK 14's upload was silently relying on a policy that didn't exist; added a new migration defining insert/select policies keyed off the `{report_id}/...` path prefix via `storage.foldername()`, reusing BLOCK 08's `owns_report()`/`has_role()` helpers so the object-level and row-level authorization shapes stay identical. Since Reporters have no UPDATE policy on `reports` or INSERT policy on `analysis_jobs` at all (by original BLOCK 08 design — these are internal orchestration writes, not Reporter actions), the endpoint's status-transition (`submitted` → `evidence_uploaded` → `analysis_queued`) and job-enqueue steps use the service-role client, documented inline as the narrow, legitimate exception this represents (Reporter authorization for the upload itself is still fully enforced by the RLS-scoped client beforehand). The Verifier evidence-review screen doesn't exist yet (still a BLOCK 11 nav stub, real queue UI is a later block's scope) — user-approved decision to satisfy "verifier sees quality/duplicate warnings" via a role-RLS-gated `GET /api/reports/:id/evidence` endpoint returning `is_duplicate_hash`/dimensions per evidence row, rather than building premature UI outside this block's stated scope.
**Consequences:** Added a `unique(report_id, sha256_hash)` constraint on `report_evidence` plus an upsert-on-conflict in the endpoint so a replayed sync attempt (network drop after the server-side work completed) doesn't create a duplicate row for the same report+file — a different scope than the separate `sha256_hash` index used for the cross-report duplicate check. `server-only`-marked modules (needed so an accidental client-bundle import fails the build) can't be unit-tested under plain vitest since the real package throws unconditionally outside Next.js's module graph — added a vitest alias substituting a no-op stub for tests only, letting `lib/evidence/*`'s pure logic be tested directly rather than requiring a live Next.js server. 26 new tests added (`magic-bytes.test.ts`, `process.test.ts`, `perceptual-hash.test.ts`, `validate-upload.test.ts`, plus 2 more in `sync-replay.test.ts` covering the endpoint's permanent/retryable classification) — full suite now at 104/104. `pnpm lint`/`typecheck`/`build`/`test` all clean; live server check confirmed the three new routes (`/api/reports/evidence`, `/api/reports/[id]/evidence`, `/api/reports/evidence/[id]/signed-url`) correctly gate behind auth (307 to `/masuk`, consistent with every prior block given no live Supabase session this session). Noticed during verification that `SUPABASE_SERVICE_ROLE_KEY` (as a Zod schema field-name string, not a secret value) appears in a client bundle chunk — traced to `packages/domain/src/env.ts` defining both the server and client env schemas in one file that `env.client.ts` imports from wholesale; confirmed no actual secret *value* reaches the bundle (checked the literal `.env` value directly), so this is a pre-existing code-hygiene gap predating this block, not a new vulnerability — flagged here rather than fixed, since splitting that file is unrelated scope.
**Status:** Accepted

### 2026-07-17 — Report BFF/domain services, state transitions via domain commands, and audit (BLOCK 16)

**Context:** BLOCK 16 required typed BFF/domain services for create report, own list/detail, verifier queue/detail, coordinator operational reports, additional information, valid state transitions, and archive — using shared Zod schemas, an `ApiResult` envelope, stable error codes, server role checks, transactions, pagination/filters, request IDs, and audit events, with the explicit constraint that no arbitrary status-update endpoint may exist: every state change must go through a domain command that validates actor/preconditions. Plus `docs/api/REPORTS_API.md` and tests.
**Decision:** Built a new `lib/api/` kernel (`ApiResult<T>`/`ApiSuccess`/`ApiFailure`, a 9-code `ApiErrorCode` union with a canonical HTTP-status map, `ApiError` for services to throw, `respondOk`/`respondError` to translate any thrown value — `ApiError`, `ZodError`, or unknown — into the envelope, and `resolveRequestId` reusing an inbound `x-request-id` or minting one) — every route in this block returns exactly this envelope. Added a non-redirecting `requireApiRole`/`requireApiPermission` in `lib/api/authorize.ts`, deliberately separate from `lib/auth/server.ts`'s page-context equivalents (which call `redirect()`, wrong for a JSON API caller). **User-approved architectural decision**: state transitions are implemented as two new `SECURITY DEFINER` Postgres RPC functions (`submit_verification_decision`, `archive_report`, following the exact `claim_analysis_jobs`/`append_audit_event` pattern from BLOCK 08) rather than sequential app-layer calls with manual rollback — each function performs its actor check (internal `has_role` guard), precondition check (current status must be a valid source state per `STATE_MACHINES.md`), the domain-specific insert (`verification_reviews`), the `reports.status` update, and the `append_audit_event` call atomically in one implicit transaction; TypeScript never computes a target status, only translates the RPC's Postgres error code (`42501`→forbidden, `P0002`→not_found, `P0001`→invalid_transition) into the stable `ApiErrorCode` vocabulary. "Additional information" is the `request_info` verification decision (per `STATE_MACHINES.md`'s existing model), not a separate table/endpoint — it keeps the report in `needs_manual_review`, annotated via the decision's own notes field. Archive required extending `PermissionAction`'s existing 8-verb legend: **user-approved decision** to model it as `report:configure` (reusing the existing "configure" verb, since AGENTS.md lists "retention" under System Administrator's configuration-domain allowances) rather than adding a 9th verb to RBAC_MATRIX.md for one call site — `archive_report()` only accepts `verified`/`rejected` source statuses, structurally preventing it from being used to bypass Verifier review. **User-approved scope decision**: `/api/reports` (POST)'s existing idempotent-upsert logic (proven, tested, load-bearing for offline sync-replay) was preserved but moved into `lib/reports/service/create.ts` and re-shaped to return the new `ApiResult` envelope — `lib/offline/sync-replay.ts` was updated in the same block to parse that envelope and classify retryability from the stable error-code vocabulary (`unauthenticated`/`rate_limited`/`internal_error` retryable, everything else permanent) instead of the old ad hoc `permanent` boolean field. `listReports` is one shared query core for all three list surfaces (own/queue/operational) — it applies zero role logic itself, trusting RLS entirely as the authorization boundary for "which rows," with only a `baseStatuses` narrowing option for UX (e.g. the Verifier queue only requests `analysis_completed`/`needs_manual_review`, never exposing every status RLS would technically let a Verifier read). `getReportById` is shared between the Reporter's own-detail and Verifier's detail routes for the same reason — a row not visible under RLS returns `not_found`, deliberately indistinguishable from a nonexistent id.
**Consequences:** Each domain-service function takes an explicit `SupabaseClient` argument rather than importing a module-level client, specifically so it's unit-testable against a fake client double (`lib/reports/service/test-support/fake-db.ts`, a minimal chainable-query-builder fake covering only the methods actually called) without a live database — this pattern should be followed by any future domain-service module. `packages/domain` gained its first real test suite (`vitest`, previously "no tests yet") for the new shared Zod schemas and pagination helpers. Added 40 new tests across `lib/api/*.test.ts`, `lib/reports/service/*.test.ts`, `lib/auth/permissions.test.ts` (new `report:configure` cross-role-denial cases), and `packages/domain/src/*.test.ts` — plus updated all `sync-replay.test.ts` fixtures to the new envelope shape (that file's own test coverage was otherwise unchanged, just its mocked response bodies). Total: 134 tests in `apps/web` + 20 in `packages/domain` = 154, all passing. `pnpm lint`/`typecheck`/`build`/`test` clean in both packages; live server check confirmed all 8 new/changed routes correctly gate behind auth (307 to `/masuk`, consistent with every prior block given no live Supabase session this session) — the RPC functions' actual runtime behavior (actor/precondition enforcement, atomicity) is therefore reviewed manually and exercised only indirectly (via the domain-service tests' mocked Postgres-error-code translation), not verified against a live database, consistent with every prior block's stated limitation.
**Status:** Accepted

### 2026-07-17 — Durable Dexie/IndexedDB offline queue, replacing BLOCK 12's mock (BLOCK 13)

**Context:** BLOCK 13 required implementing the real `mboyo-offline` IndexedDB database (Dexie) with five tables (reportDrafts, reportQueue, evidenceBlobs, syncAttempts, appMetadata), a full queue API (save/restore/clear draft, enqueue, list, counts, claim, mark synced/failed/conflict, retry, delete, prune, evidence cleanup), exponential backoff with jitter, a single-flight lock, permanent-vs-retryable error classification, quota/storage-estimate handling, and fake-indexeddb tests — replacing BLOCK 12's explicitly-temporary in-memory mock (`lib/reports/mock-online-repository.ts`) entirely, per that mock's own instructions ("must be deleted, not extended").
**Decision:** Built the Dexie schema with `client_report_id` declared `&` (unique) at the schema level — the idempotency guarantee lives in IndexedDB's own constraint enforcement, not just application-level checking, and `enqueueReport()` additionally handles the race where two concurrent enqueue calls both pass an existence check before either writes (re-reads and returns the winner rather than surfacing a spurious duplicate-key error). `claimPendingItems()` runs inside a Dexie transaction and is meant to be wrapped in `withSyncLock()` (Web Locks API, cross-tab-safe, with an in-memory fallback) by callers — the transaction alone only guards against double-claiming within one JS realm, not across tabs/service-worker contexts, and the doc comments say so explicitly rather than overclaiming the guarantee. Backoff uses full jitter (`random() * min(cap, base*2^attempts)`, not a fixed exponential value) specifically so a shared outage doesn't cause every queued item to retry in lockstep on reconnect. `conflict` status is modeled as genuinely distinct from `failed`: conflicts are never auto-retried by `claimPendingItems` (only `retryItem`, which resets to `pending`, can move one forward) — this exact behavior was verified by a test that initially failed and caught a real bug (see Consequences). `markFailed`/`markConflict`/`markSynced` never touch `payload`/`evidence_blob_id` on failure, satisfying "preserve failed data" literally. Evidence cleanup is two-phase: `clearEvidenceAfterConfirmedUpload` frees the (often multi-megabyte) Blob immediately on confirmed server upload while the queue row stays for visibility, and `pruneSynced` removes the row itself only after a 24-hour retention window — "delayed cleanup after sync," not immediate. The BLOCK 12 `OfflineReportRepository` interface is now implemented by `lib/offline/dexie-report-repository.ts`; `use-report-repository.ts` was repointed and the mock adapter file deleted outright (not deprecated in place).
**Consequences:** A test-driven bug was caught and fixed during verification: `claimPendingItems`'s initial filter (`item.status === "pending" || !item.next_retry_at || ...`) accidentally matched `conflict` items with a null `next_retry_at`, which would have let a real conflict get silently auto-retried — directly contradicting the "conflict requires explicit resolution" design intent stated in `markConflict`'s own doc comment. Fixed by excluding `conflict` from the claimable status set entirely. Also fixed two lint errors surfaced during verification (an `async` test hook with no `await`, and an unused destructured variable) rather than suppressing. `fake-indexeddb/auto` is loaded via a vitest `setupFiles` entry so Dexie runs against a real (in-memory, spec-compliant) IndexedDB implementation under Node, not a hand-rolled stub — 37 new tests across `queue.test.ts`/`backoff.test.ts`/`errors.test.ts` cover persistence-across-simulated-restart (db close+reopen), native Blob round-tripping, enqueue idempotency (including the concurrent-race case), claim exclusivity, backoff growth/jitter/cap, manual retry bypassing backoff, conflict non-auto-retry, synced-item evidence cleanup, and delayed pruning. Full suite now at 68/68 passing; `pnpm lint`/`typecheck`/`build` all clean; confirmed via live dev server that reporter routes still execute without a server-side crash (no real Supabase backend reachable this session, so full authenticated click-through remains unverified, consistent with prior blocks).
**Status:** Accepted

### 2026-07-17 — GPS capture, manual MapLibre pin, and server-side location trust (BLOCK 17)

**Context:** BLOCK 17 required full location capture: high-accuracy geolocation with timeout/maximumAge, a real manual MapLibre pin (replacing BLOCK 12's plain-number-input stand-in), address fallback, event-boundary warning without blocking submission, and server-side coordinate validation/PostGIS storage/distance-to-event-center/suspicious-pattern flags for human review (never automatic rejection) — plus a Verifier UI surfacing source/accuracy/timestamp/boundary/distance, an optional reverse-geocoder adapter/cache, and strict `[longitude, latitude]` GeoJSON ordering throughout.
**Decision:** **User-approved decision**: installed `maplibre-gl` and built a real draggable-pin map (`components/map/MapPin.tsx`, dynamic-imported with `ssr:false`) rather than deferring it further — one shared component for the Reporter's draggable manual pin, the GPS step's read-only accuracy-circle display, and the Verifier's read-only location panel, so the `[longitude, latitude]` convention (MapLibre's own native order) is enforced in exactly one place for every map interaction. **User-approved decision**: built the optional reverse-geocoder as a `ReverseGeocoder` adapter interface with a `NullReverseGeocoder` (always returns null, explicitly labeled per AGENTS.md's demo-fallback-disclosure rule) active whenever `MAPTILER_API_KEY` is unset, and a cache-first `MapTilerReverseGeocoder` backed by a new `reverse_geocode_cache` table (RLS enabled with zero policies — deny-all except service-role) when configured — a resolved address is always a display convenience, never an authoritative location source. State transitions follow BLOCK 16's established RPC pattern: `record_geolocation_observation()` (new migration, `SECURITY DEFINER`, mirrors `append_audit_event`/`submit_verification_decision`) is the only sanctioned write path — validates ownership and coordinate range, constructs the PostGIS point via `st_makepoint(longitude, latitude)` (longitude first, the one call site where reversing the order would silently misplace a report without looking obviously wrong near the equator), computes distance-to-geofence-centroid/`outside_event_boundary`/`suspicious_pattern_flags`/`confidence_signal` (the BLOCK 08 column left unpopulated until now — a simple, explainable source-plus-accuracy-tier heuristic, never a claim of ground truth per this block's "never describe GPS as guaranteed truth" requirement), inserts, and appends a `report.location_recorded` audit event — atomically. **User-approved architectural decision**, made after discovering the wizard's whole submission flow is offline-first with no synchronous point where a server response could reach the Reporter mid-flow: location upload was wired into `lib/offline/sync-replay.ts`'s existing report→evidence sync sequence (report, then evidence, then location, each only after the prior step's server-side row is confirmed to exist) rather than attempting a best-effort direct POST from the wizard — this makes the event-boundary/suspicious-pattern signals Verifier-facing only in this block's scope, which is the honest consequence of the existing async architecture, not a shortcut. A `location_synced` boolean was added to the offline queue schema, mirroring `evidence_blob_id`'s clear-after-confirm pattern, so a retried sync pass never records a duplicate observation. Added `report_evidence`-adjacent RBAC entries (`geolocation_observation:create`/`read`) transcribed directly from RBAC_MATRIX.md's existing row (Reporter C+R own, Verifier R all, Coordinator R verified-only, Auditor R) — no schema/matrix change needed since BLOCK 08 already specified this entity fully. Two generated columns (`longitude`, `latitude`, via `st_x`/`st_y` on the stored `geography(Point)`) were added since PostgREST cannot decode a raw PostGIS point through an ordinary column select — needed for the API layer and Verifier map to read coordinates back at all.
**Consequences:** No dedicated Verifier report-detail screen exists yet (`/verifier/laporan` remains a BLOCK 11 nav stub) — following the same precedent BLOCK 15/16 set, `LocationTrustPanel` (source/accuracy/timestamp/boundary/distance/confidence/flags display) and its backing `GET /api/reports/:reportId/location` route were built as the data-correct, RBAC-correct pieces ready to drop into that future screen, rather than forcing a premature full-screen wiring into an unrelated stub page. Added `docs/architecture/LOCATION_TRUST.md` documenting the full pipeline, the never-auto-reject principle, and known limitations. New tests: `packages/domain/src/reports.test.ts` (submitLocationObservationSchema — range/refine validation), `lib/reports/service/location.test.ts` (RPC error-code translation, numeric-string-to-number conversion), and 5 new `sync-replay.test.ts` cases (location upload success/skip-when-absent/retryable-failure/permanent-conflict, verifying `location_synced` state transitions correctly in each case). Full suite: 147 tests in `apps/web` (was 134) + 30 in `packages/domain` (was 20), all passing; `pnpm lint`/`typecheck`/`build`/`test` clean in both packages. `maplibre-gl` installed without native-binary/build-script friction (unlike `esbuild`/`sharp` in earlier blocks). Live server check confirmed the new `/api/reports/:id/location` route and the wizard's manual-location step both gate/render correctly (307 to `/masuk` for the API route, consistent with every prior block given no live Supabase session this session — the RPC's actual coordinate-insertion and PostGIS distance computation is therefore reviewed manually and exercised only via mocked Postgres-error-code translation, the same stated limitation as every prior block's RPC work).
**Status:** Accepted

### 2026-07-18 — ML dataset governance: cards, licensing manifest, and a real prepare/deduplicate/audit/split pipeline (BLOCK 18)

**Context:** BLOCK 18 required building ML data governance for the ground-level damage-severity classifier: `ml/DATA_CARD.md`, `ml/LABELING_GUIDE.md`, `ml/ETHICS_AND_PRIVACY.md`, `ml/configs/dataset.yaml`, a source/license manifest, and real (not stub) `prepare`/`audit`/`split`/`deduplicate` scripts with tests — covering immutable raw data, corruption detection, duplicate/near-duplicate detection, face/document privacy flags, geographic/source-aware splitting, class audit, dimension audit, label agreement, optional adjudication, and reproducible hashes. The prompt's explicit warning — ground-level citizen photos are a different visual domain from satellite/aerial imagery, and performance claims must never transfer between them — had no prior repo language to align with (confirmed via research: zero mentions of satellite-vs-ground imagery anywhere in the codebase before this block).
**Decision:** Extended the existing `ml/` skeleton from an earlier block (stub `prepare_data.py`/`train.py`/`evaluate.py`/`export_onnx.py`, empty `ml/data/`, a `pyproject.toml` with ruff/mypy-strict conventions) rather than restructuring it — `prepare_data.py` was rewritten from its "not yet implemented" stub into a real implementation; `train.py`/`evaluate.py`/`export_onnx.py` remain untouched stubs, correctly out of this block's scope (data governance, not training). **User-approved scope decision**: `dataset.yaml` covers dataset/split/quality/dedup/privacy/licensing config only — the release-gate thresholds (macro-F1/destroyed-recall/calibration-error) `SUCCESS_METRICS.md` explicitly defers to "the current `ml/configs/` release criteria file" were left undefined, deferred to the future training/evaluation block where real numbers can be chosen with actual model context rather than guessed now. Built a shared `ml/src/data_governance/` package (config loading with fail-fast validation, a CSV-based `ManifestRow` schema shared by all four pipeline stages, SHA-256/average-hash image utilities reusing the same aHash technique as `apps/web`'s BLOCK 15 evidence pipeline, and a `SOURCES.md` Markdown-table parser) so no script duplicates schema or hashing logic. `prepare_data.py` structurally enforces the licensing policy: a raw-data source subdirectory not registered in `SOURCES.md` with a non-blank, non-"unclear" license is excluded and reported, never silently ingested — this is the concrete mechanism behind "never auto-download unclear-license datasets," not just a documented rule. `deduplicate.py` marks exact duplicates (`duplicate_of`) and near-duplicate candidate groups (`near_duplicate_group`) but never deletes a row — detection and reporting only, per this block's explicit requirement that near-duplicates are a human-review signal, not an auto-delete trigger. `split.py` groups by `(source_id, geographic_group)` so no group is ever spread across train/val/test (preventing the same disaster event's near-identical images from leaking between splits and silently inflating a metric — directly extending `RISK_REGISTER.md` risk #5's "image sources represented unevenly" concern), assigns groups via a seeded deterministic greedy balancer (same manifest + same seed always produces the same split), excludes privacy-flagged rows by default (explicit `--include-privacy-flagged` opt-in only, never a silent config flip), and records the input manifest's SHA-256 in its report — the "reproducible hashes" requirement, tying a split's output to the exact byte-identical input that produced it. `audit.py` computes class distribution (preferring `adjudicated_label` over the raw `label`), dimension statistics, ordinal-distance-aware label agreement (severity classes have a natural order, so a `minor_damage`/`destroyed` disagreement is scored differently from `minor_damage`/`major_damage`), and writes an adjudication queue only for disagreements exceeding a one-step ordinal gap — per `LABELING_GUIDE.md`'s explicit design, never auto-resolved by majority/averaging. Added a `confidence_signal`-adjacent but distinct concept: `unknown` (a genuine label a human can assign) is kept structurally separate from the trained model's future abstention behavior — `DATA_CARD.md` calls this out explicitly since `SUCCESS_METRICS.md`'s existing abstention-rate definition could otherwise be confused with this dataset's `unknown` class. RISK_REGISTER.md gained a new risk #12 (Training Data Licensing and Provenance) — no prior risk entry covered dataset sourcing/licensing directly (Model Bias and Sensitive Imagery are adjacent but distinct).
**Consequences:** Found and fixed a real path bug during manual verification: `dataset.yaml`'s `paths.reports: ../reports` resolved one directory too high (to the repo root's non-existent `reports/`, not `ml/reports/`) because `config.py`'s `config_root` is already `ml/` — fixed to `reports: reports`, re-verified the report lands in the correct `ml/reports/` directory. Added `pyyaml`/`types-PyYAML` to `ml/pyproject.toml` (previously absent despite config-loading being a de facto requirement) and a `[tool.pytest.ini_options] pythonpath = ["src"]` entry so `pnpm run ml:test`/`ml:lint`/`ml:typecheck` (three new root `package.json` scripts, alongside `ml:deduplicate`/`ml:audit`/`ml:split`) work from the repo root without a manually-set `PYTHONPATH` env var. Wrote 60 new pytest tests (`ml/tests/test_{config,imaging,manifest,sources,prepare_data,deduplicate,audit,split}.py`) using Pillow-generated synthetic fixtures (checkerboard patterns for meaningful perceptual-hash tests, since a flat/solid-color image's aHash is degenerate — every pixel equals the mean regardless of color) — no real photograph appears anywhere in the test suite, and every fixture is isolated to `tmp_path`, never touching the real `ml/data/`. Manually verified all four pipeline stages end-to-end via `pnpm run ml:prepare/deduplicate/audit/split` from a clean state, confirming graceful zero-row exits at every stage (exit code 0, header-only manifest/no-data report, never an error) per this block's explicit "empty data must exit gracefully" requirement — this is the honest current state, since `ml/data/raw/` and `data/manifests/SOURCES.md`'s source table remain genuinely empty (no real training data exists yet). `ruff check`/`ruff format`/`mypy --strict`/`pytest` all clean across the full `ml/` tree (24 source files, 60 tests).
**Status:** Accepted

### 2026-07-19 — Model benchmark and training: two-phase methodology, composite selection rule, CPU smoke test (BLOCK 19)

**Context:** BLOCK 19 required a fair benchmark of MobileNetV3-Large/EfficientNetV2-S/ConvNeXt-Tiny across macro-F1, destroyed recall, per-class metrics, expected calibration error, CPU latency, model size, and robustness to poor image quality, plus config-driven training modules (model factory, losses, metrics, early stopping, deterministic seeds, experiment logging, checkpoints), transfer learning at 224/288px with ImageNet normalization, class-weighted/focal-loss comparison, an optional sampler, mixed precision when available, a CPU smoke mode, early stopping on validation macro-F1, no fake metrics, an optional two-stage quality/relevance gate, and a documented composite selection rule. As with BLOCK 18, `ml/data/raw/` remains genuinely empty — no real training photographs exist yet in this environment.
**Decision:** **User-approved decision**: built the full real pipeline (training/benchmark code reads through BLOCK 18's `prepare_data.py`/`split.py` manifest exactly as designed) rather than fabricating an illustrative result — with zero real data, `run_benchmark.py`'s default path detects this and exits 0 with an honest "no data" message, while `--smoke-test` proves the pipeline end-to-end against a small, explicitly-labeled synthetic checkerboard dataset generated fresh each run. **User-approved decision**: a two-phase benchmark methodology rather than an exhaustive grid — Phase 1 fixes architecture=mobilenet_v3_large (cheapest) at 224px and sweeps all loss functions (cross-entropy, class-weighted cross-entropy, focal loss) plus a `WeightedRandomSampler` on/off candidate, picking a winner by validation macro-F1; Phase 2 sweeps all 3 architectures × 2 resolutions (224/288) using Phase 1's winning loss/sampler choice — 6 full training runs instead of 18-36, with the methodology restated in both `ml/BENCHMARK_METHODOLOGY.md` and the generated report so it's auditable, not an unexplained shortcut. Built `ml/src/training/` as a new sibling package to `data_governance/` (model factory, focal loss implemented from scratch and verified numerically identical to cross-entropy at gamma=0, hand-implemented 10-bin Expected Calibration Error since scikit-learn has none, deterministic seeding verified bit-for-bit reproducible, early stopping keyed on validation macro-F1 specifically — never validation loss, since loss can improve on the majority class while macro-F1 plateaus). The composite selection rule (`selection_rule.py`) applies hard gates first — destroyed_recall must be computable and ≥0.5, CPU p95 latency must be ≤2000ms — excluding any failing candidate from ranking entirely regardless of its other scores, then ranks gate-passing candidates by a weighted composite (macro-F1 0.40, destroyed recall 0.25, calibration 0.15, latency 0.10, model size 0.05, robustness 0.05); the module docstring documents these weights as a defensible starting point, not empirically derived, since no real benchmark has run yet. Robustness degradation (Gaussian blur, simulated low light, JPEG compression artifacts) was implemented in pure Pillow rather than `cv2`, because `opencv-python-headless` (a listed dependency) currently fails to import in this environment with a NumPy 2.x ABI incompatibility (`_ARRAY_API not found`) — disclosed in `BENCHMARK_METHODOLOGY.md`'s Known Limitations rather than silently worked around by pinning NumPy down. The optional two-stage quality/building-relevance gate is documented but left non-runnable (`two_stage_gate.enabled: false`), since BLOCK 18's manifest schema has no `is_valid_subject`-style column yet — deferred to a future block that extends the labeling schema first, rather than inventing a column now.
**Consequences:** Found and fixed three real bugs during manual verification, none of them silently worked around: (1) `smoke_test.synthetic_image_count_per_class: 4` in `training.yaml` produced a `{train:3, val:1, test:0}` split (the ~60/20/20 split math rounds the test split to zero) — caught by `run_smoke_test`'s own defensive check, which correctly exited 1 with a clear message rather than proceeding with a missing split; fixed by bumping the config value to 6. (2) The initial `--smoke-test` path ran the *full* production grid (3 architectures × 2 resolutions × up to 4 loss/sampler candidates at `max_epochs: 30`) against tiny synthetic data, timing out past 300 seconds — fixed by adding `derive_smoke_test_training_config()`, which scopes the config to exactly 1 architecture/1 resolution/1 loss function/no sampler and the `smoke_test` section's own epoch/batch-size values, bringing smoke-test wall-clock time down to ~7-10 seconds. (3) The smoke-test report initially displayed the real, unused `ml/data/manifests/split.csv` path instead of the actual temporary synthetic manifest used — fixed by adding a `manifest_path_override` parameter threaded through `run_benchmark()`. Wrote 72 new pytest tests across `ml/tests/test_{training_config,losses,metrics,early_stopping,checkpoints,experiment_log,selection_rule,training_dataset,robustness,smoke_test_data,run_benchmark_cli}.py`, including a `@pytest.mark.slow`-marked end-to-end CLI test that runs `run_smoke_test()` for real (registered in `pyproject.toml`'s new `markers` list so `pytest -m "not slow"` stays fast by default). `models.py`, `seeding.py`, `train_loop.py`, `latency.py`, `benchmark.py`, and `report.py` were each manually verified via direct script during development (documented in-session) and are exercised indirectly by the CLI integration test, but have no dedicated unit test files of their own — a known gap, not silently hidden, left for a future block given this one's scope was already substantial. Full `ml/` suite: 132 tests passing (60 from BLOCK 18 + 72 new), `ruff check`/`ruff format`/`mypy --strict` all clean across the full tree, no stray report/manifest artifacts left behind after verification.
**Status:** Accepted

### 2026-07-20 — Untouched-test-set evaluation, calibration, abstention, Grad-CAM, release gates, and export (BLOCK 20)

**Context:** BLOCK 20 required untouched-test-set evaluation (confusion matrix, classification report, macro/micro/weighted F1, destroyed recall, PR curves, calibration curve, ECE, latency percentiles, model size, robustness by blur/darkness/compression/resolution, abstention rate), probability calibration with per-class thresholds, a `needs_manual_review`/abstain policy driven by low confidence/high entropy/OOD/quality-failure signals, Grad-CAM explainability for verifiers with a non-causal disclaimer, release gates that register a model as advisory-only if they fail, and ONNX/TorchScript/metadata-JSON/preprocessing-spec/checksum/runtime-benchmark export — never evaluating on training or validation data as final evidence. As with BLOCK 18/19, no real training data or trained model exists yet in this environment.
**Decision:** **User-approved decision**: `ml/configs/release_criteria.yaml` (the release-criteria file `SUCCESS_METRICS.md`'s Release Gate section explicitly defers to) was populated with documented, non-empirical starting thresholds (`macro_f1_min: 0.75`, `destroyed_recall_min: 0.60`, `calibration_error_max: 0.10`, `min_test_set_sample_count: 30`) rather than left undefined — mirroring `selection_rule.py`'s (BLOCK 19) own provisional-threshold precedent, explicitly disclosed as not derived from real data. Built `ml/src/training/{calibration,abstention,gradcam,release_config,release_gate,export,evaluate,evaluation_report}.py` as new modules alongside BLOCK 19's `training/` package, plus `ml/src/run_evaluate.py`/`ml/src/run_export.py` CLI entry points (replacing BLOCK 18's placeholder `ml/src/evaluate.py`/`ml/src/export_onnx.py` stubs entirely, since this block is their real implementation). Extended (not duplicated) BLOCK 19's `metrics.py` with confusion matrix, micro/weighted F1, PR curves, and calibration-curve points, and `robustness.py` with a fourth degradation (`low_resolution`, downscale-then-upscale) alongside the existing three. Calibration (`calibration.py`) uses temperature scaling (Guo et al., 2017) fit via LBFGS on the validation split only, verified to preserve argmax (accuracy/macro-F1 unaffected by calibration); per-class thresholds replace BLOCK 19's single global `abstention_confidence_threshold` with one threshold per class, fit to the lowest confidence value meeting a target validation-set precision, falling back to the global default when too few validation samples exist to fit reliably. Abstention (`abstention.py`) combines four independent signals (low confidence, high entropy, a deliberately simple feature-centroid OOD proxy, and a quality-gate floor) — any one signal triggers abstention, never averaged against the others, with every triggering reason surfaced rather than hidden. Grad-CAM (`gradcam.py`) is hand-implemented in pure PyTorch (forward/backward hooks on `.features`' last layer, gradient-weighted CAM, ReLU, bilinear upsample) with a pure-Pillow heatmap overlay (hand-rolled blue→yellow→red colormap + `Image.blend`) rather than the cv2-based overlay most Grad-CAM libraries (including `pytorch-grad-cam`) assume, since `opencv-python-headless` remains unusable in this environment per BLOCK 19's finding; every result carries a non-causal disclaimer (`GRAD_CAM_DISCLAIMER`) that must be surfaced verbatim wherever an overlay is shown. The release gate (`release_gate.py`) applies macro-F1/destroyed-recall/calibration-error/minimum-sample-count gates, never traded off against each other, marking a model `is_advisory_only` (a field name deliberately matching `model_predictions.is_advisory_only` in the Supabase schema) on any failure. Export (`export.py`) produces ONNX (verified via `onnx.checker.check_model` and a real ONNX Runtime inference pass) and TorchScript artifacts, a metadata JSON whose field names deliberately mirror `model_registry_entries`/`model_evaluations`' exact Supabase column names (confirmed via research into the existing schema before writing the field names, not guessed), a preprocessing spec, SHA-256 checksums, and a runtime latency benchmark measured against the actual exported ONNX artifact via ONNX Runtime (not the raw PyTorch model, which BLOCK 19's `latency.py` already covers for architecture-selection purposes).
**Consequences:** Found and fixed a real environment gap: `onnx` (distinct from `onnxruntime`) was listed in `ml/pyproject.toml` since BLOCK 19 but was never actually installed (`ModuleNotFoundError: No module named 'onnx'`, confirmed directly) — installed via pip; the `tensorflow-intel`/`ml-dtypes` version-conflict warnings this triggered are about an unrelated global package, not this project's dependencies. Found that PyTorch's new default (dynamo-based) ONNX exporter requires `onnxscript`, not a listed dependency, and fails with `ModuleNotFoundError` in this environment — worked around by using the legacy exporter path explicitly (`dynamo=False`), disclosed in `EVALUATION_METHODOLOGY.md` since a future PyTorch upgrade may remove that path entirely. Found and fixed a genuine Grad-CAM bug (not a pre-existing one): a model with every parameter frozen (`set_backbone_trainable(model, False)`, BLOCK 19's transfer-learning pattern) has nothing in its forward-pass graph requiring grad, so `.backward()` failed with `element 0 of tensors does not require grad` — reproduced directly, then fixed by forcing the input tensor itself (never a model parameter) to require grad inside `compute_grad_cam`. Deleted BLOCK 18's placeholder `ml/src/evaluate.py`/`ml/src/export_onnx.py` stub files entirely (superseded, not partially extended) and repointed `package.json`'s `ml:evaluate`/`ml:export` scripts at the new `run_evaluate.py`/`run_export.py`, adding `ml:evaluate:smoke-test`/`ml:export:smoke-test` mirroring BLOCK 19's `ml:benchmark:smoke-test` convention. Extended `.gitignore` to cover export-run artifacts (`ml/models/*_metadata.json`, `ml/models/*_gradcam_samples/`) alongside BLOCK 18's existing `*.onnx`/`*.pt`/`*.pth` entries. Wrote 88 new pytest tests across `ml/tests/test_{metrics (extended), robustness (extended), calibration, abstention, gradcam, release_config, release_gate, export, evaluate, evaluation_report, run_evaluate_cli, run_export_cli}.py`, including `@pytest.mark.slow` end-to-end CLI tests for both `run_evaluate.py --smoke-test` and `run_export.py --smoke-test` that train a real tiny model on synthetic data, evaluate it, and (for export) produce real ONNX/TorchScript/metadata/Grad-CAM-sample artifacts — verified by inspecting the actual generated files/JSON payloads, not just a zero exit code. One disclosed known gap carried into this block: the per-sample quality score `run_evaluate.py`'s CLI feeds into the abstention decision is a neutral `1.0` placeholder (no real per-report quality-score pipeline is wired into this CLI yet), and the OOD feature-centroid detector exists and is tested but is not yet wired into the CLI (no feature-vector extraction call site yet) — both disclosed in `EVALUATION_METHODOLOGY.md` rather than silently hidden. Full `ml/` suite: 212 tests passing (132 from BLOCK 18/19 + 80 new), `ruff check`/`ruff format`/`mypy --strict` (across both `src/` and `tests/`) all clean, no stray model/report artifacts left behind after verification.
**Status:** Accepted

### 2026-07-21 — FastAPI ML API, job worker, retry/dead-letter/reclaim lifecycle, and internal contracts (BLOCK 21)

**Context:** BLOCK 21 required implementing `apps/ml-api` (GET `/health`/`/ready`/`/model-info`, POST `/validate-image`/`/predict`/`/explain`/`/batch-predict` internal-only) and `apps/worker` (safely claims database jobs, fetches private evidence, validates, runs local inference, stores prediction/explanation, transitions the report, retries transient failures, dead-letters after a limit, heartbeats, audits) — with model load-once, readiness waiting for the model, a deterministic DEMO_MODE-only fallback, model-artifact/evaluation registration, and an acceptance test that a report becomes a job becomes a worker-processed prediction, with a worker restart never duplicating results. Both apps already existed as scaffolding from an earlier block (FastAPI/pydantic-settings skeleton with a working internal-token auth dependency for ml-api; an asyncio heartbeat-only skeleton for worker) — this block filled in the real implementation, not a rewrite.
**Decision:** **User-approved decision**: broadened `DEMO_MODE`'s previously UI-only scope (per `docs/architecture/SYSTEM_ARCHITECTURE.md`, which said demo mode "never mocks ML inference") to also permit `apps/ml-api` to serve a clearly-labeled deterministic fallback prediction (`is_demo_fallback: true`, hash-of-image-bytes-selects-the-class, never random) when no active model artifact is loaded — documented explicitly in `SYSTEM_ARCHITECTURE.md` as a genuine scope change rather than left as a silent contradiction with AGENTS.md's general demo-fallback-labeling rule, and kept structurally distinct from `is_advisory_only` (a *loaded* model that fails its release gate). **User-approved decision**: retry/dead-letter limits are `MAX_ATTEMPTS = 3` and a `PROCESSING_LEASE = 300` seconds (5 minutes) — documented as a provisional, non-empirical starting point (no production incident history exists yet), mirroring BLOCK 19/20's own "provisional threshold, disclosed as such" precedent. **User-approved decision**: since standard ONNX Runtime inference sessions expose no gradients (apps/ml-api serves ONNX-only, never imports PyTorch/`ml/` directly per AGENTS.md's architecture boundary), `/explain` implements occlusion-based sensitivity (Zeiler & Fergus, 2014) — a genuinely different, black-box-compatible technique from BLOCK 20's true PyTorch Grad-CAM — rather than adding a full PyTorch dependency just to reproduce Grad-CAM exactly; every response is honestly labeled "occlusion-based sensitivity, not Grad-CAM," never mislabeled.

New Postgres migration (`20260719061204_worker_job_lifecycle.sql`) adds four `SECURITY DEFINER` functions completing the job lifecycle `claim_analysis_jobs` (BLOCK 08) started: `reclaim_stale_analysis_jobs` (requeues, or dead-letters past `max_attempts`, jobs stuck `processing` past the lease window — the lease-expiry mechanism ADR 0003 described but never implemented in SQL until now), `mark_report_analysis_running` (the report-side half of `analysis_queued -> analysis_running`, called once per claimed job since the batch-claiming RPC itself has no per-report knowledge), `record_analysis_result` (the single atomic write for a successful inference: `model_predictions` + optional `model_explanations` + `analysis_jobs.status='done'` + report transition to `analysis_completed`/`needs_manual_review`, all in one transaction), and `fail_analysis_job` (records a transient failure, requeuing or dead-lettering). All four cross-check that `p_report_id` actually matches the job's own `report_id` before writing, guarding against a caller-side bug silently mutating the wrong report. `append_audit_event` (BLOCK 08) needed no change — it already attributes to `current_profile_id()`, which correctly resolves `NULL` under a service-role connection (no JWT/`auth.uid()`), giving the "system" attribution `apps/worker`'s own audit writes need for free.

`apps/ml-api` gained: `model_registry.py` (loads the active ONNX artifact exactly once at FastAPI's `lifespan` startup — never per-request — via an `active_version.txt` indirection file naming the current version, verifying the artifact's SHA-256 checksum against BLOCK 20 export metadata before trusting it), `imaging.py` (decode/quality-check/preprocess, quality thresholds mirroring `ml/configs/dataset.yaml`), `abstention.py` (a self-contained low-confidence/high-entropy/quality-failure reimplementation of BLOCK 20's abstention logic — apps/ml-api never imports `ml/` directly, so this is deliberately a separate, parallel implementation, not a shared import), `demo_fallback.py`, `explain.py`/`heatmap.py` (the occlusion-sensitivity technique above), `origin_guard.py` (rejects any request carrying a browser-style `Origin` header not in an explicit allowlist — defense-in-depth since this service has no public ingress and is never called from a browser at all), `rate_limit.py` (a simple in-process sliding-window limiter, also defense-in-depth given the primary anti-abuse control lives upstream at the `apps/web` BFF layer per `THREAT_MODEL.md`), and `request_context.py` (mirrors `apps/web`'s `lib/api/request-id.ts` exactly: same `x-request-id` header name, same mint-or-echo semantics, so a request traced through `apps/web -> apps/worker -> apps/ml-api` keeps one correlation ID across all three languages). Every error response uses the same `{ ok: false, error: { code, message }, requestId }` envelope as `apps/web`'s `lib/api/result.ts`.

`apps/worker` gained: `supabase_client.py` (a thin `httpx`-based PostgREST/Storage wrapper — deliberately not the official `supabase-py` SDK, to keep the dependency footprint aligned with this app's existing minimal `pydantic-settings + httpx`-only style; a `SupabaseClientProtocol` lets tests substitute a lightweight fake instead of needing an `httpx.MockTransport` for every call site), `ml_api_client.py` (typed HTTP client sending `Authorization: Bearer <ML_INTERNAL_TOKEN>` and a per-job `x-request-id`), `audit.py`, `processing.py` (`process_job`: fetch evidence via service-role Storage download — never a signed URL, since this is a trusted server-to-server call, not browser-facing — validate, predict, explain (assistive, non-fatal on failure), record the result, transition the report), and `claim_loop.py` (the `claim_analysis_jobs`-driven claim loop plus a periodic `reclaim_stale_analysis_jobs` sweep, run concurrently with the existing heartbeat loop in `main.py` via `asyncio.gather`). Worker's `config.py` reuses `NEXT_PUBLIC_SUPABASE_URL` (already public, not a secret) via an explicit pydantic alias rather than inventing a second worker-only Supabase-URL variable.

**The core idempotency guarantee** ("worker restart does not duplicate results"): `model_predictions.analysis_job_id` is `UNIQUE` (BLOCK 08 schema) — `record_analysis_result` therefore raises a Postgres `unique_violation` (SQLSTATE `23505`, surfaced by PostgREST as HTTP 409) if called twice for the same job. `process_job` parses that specific error precisely (via the response body's `code` field, mirroring `apps/web/src/lib/reports/service/location.ts`'s existing `PostgrestLikeError.code`-based RPC-error-translation pattern, not a fragile substring match) and treats it as `"already_processed"` — success, not a retry trigger. Combined with `reclaim_stale_analysis_jobs`'s lease-expiry requeue, a worker that crashes mid-job is recovered by a later claim cycle, which either produces the first real result or discovers one already exists and stops cleanly — verified directly via a test reproducing exactly this 409/23505 scenario, plus a companion test verifying a malformed (non-JSON) 409 body fails safe (treated as a different, non-duplicate failure) rather than crashing the duplicate-detection logic itself.

Added `packages/domain/src/ml.ts` (Zod schemas for every ml-api request/response shape, including `severityProbabilitiesSchema` mirroring `model_predictions.severity_probabilities`'s exact database check constraint — all 5 `SEVERITY_CLASSES` present, each in `[0,1]`, summing to ~1) and extended `packages/api-client/src/ml-api.ts` (previously only `health()`) with `ready()`/`modelInfo()`/`validateImage()`/`predict()`/`explain()` — no Zod schemas for these shapes existed anywhere before this block; every Pydantic schema in `apps/ml-api/app/schemas.py` was written to mirror these field-for-field (camelCase JSON on the wire via `serialization_alias`, matching `HealthResponse`/`health.ts`'s established cross-language mirroring precedent).

**Consequences:** No live Supabase/Docker instance was available this session (`docker ps`/`npx supabase status` both failed — Docker Desktop's daemon unresponsive, a recurring environment issue already documented in `docs/architecture/LOCAL_PLATFORM.md` from earlier blocks) — the new migration's SQL was manually reviewed line-by-line (including tracing the exact acceptance-flow call sequence: evidence upload already creates the `queued` job per BLOCK 15's existing route code, `claim_analysis_jobs` claims it, `mark_report_analysis_running` transitions the report, `record_analysis_result`/`fail_analysis_job` handle the two terminal outcomes) but not executed against a live database; a future session should run `pnpm db:reset` once Docker is available to complete live verification. Everything else was verified directly: `apps/ml-api`'s full FastAPI app was exercised via `TestClient` against both a real exported ONNX model (a minimal hand-built ONNX graph fixture, not requiring `ml/`'s PyTorch dependency inside `apps/ml-api`'s own test suite) and the DEMO_MODE no-model-loaded path, confirming `/predict`/`/explain`/`/validate-image`/`/batch-predict`/`/ready`/`/model-info` all produce correctly-shaped, correctly-labeled responses, that a wrong/missing internal token is rejected, and that a browser-style `Origin` header is rejected. 41 new `apps/ml-api` tests and 25 new `apps/worker` tests all pass; `ruff format`/`ruff check`/`mypy --strict` clean across both apps' `app/`/`worker/` and `tests/` trees. Confirmed no regression in the rest of the monorepo: `packages/domain` (30 tests), `packages/api-client`, and `apps/web` (147 tests) all typecheck/lint/test clean after the new `ml.ts` schemas and `ml-api.ts` client extensions. Found and fixed two real gaps during this block's own build: `onnx`/`onnxruntime`/`numpy`/`pillow` were missing from `apps/ml-api/pyproject.toml`'s dependencies (added, plus a narrow `onnxruntime.*` mypy override mirroring `ml/pyproject.toml`'s identical exception); a middleware constructed with `get_settings()` at `add_middleware()` call time broke `app.main`'s "importable without real secrets present" convention — fixed by resolving settings fresh inside the middleware's `dispatch()` instead. Known, disclosed limitations: no live-database verification of the new SQL functions this session; the demo fallback and occlusion-sensitivity techniques are new, narrow deviations from their BLOCK 20 PyTorch-based counterparts, disclosed rather than silently presented as identical; `apps/worker`'s reclaim-sweep and claim-loop intervals (5s poll, 60s sweep) are reasonable defaults, not load-tested.
**Status:** Accepted

### 2026-07-21 — Optional Gemini verifier advisory (BLOCK 22)

**Context:** BLOCK 22 required implementing Gemini as an optional, non-authoritative Verifier advisory: local CV remains primary; local failure/abstention keeps a report in `needs_manual_review`; Gemini may produce an evidence summary, one suggested follow-up question, a non-binding hypothesis, and quality observations; a raw image is sent only when the feature is enabled, explicitly requested by the Verifier, consent/policy permits it, and external-cloud disclosure is accepted (a redacted image or derived metadata preferred otherwise); prompt-injection defense against reporter-supplied text; structured output only; a server-only API key; timeout/retry/rate/cost telemetry; a complete audit trail; never storing chain-of-thought; never automatically changing official report status; the required UI label "Analisis Tambahan Eksternal — Tidak Menentukan Keputusan Resmi"; the app must work with zero Gemini key configured; tests must mock the provider. No prior code touched Gemini at all — only env var placeholders (`GEMINI_API_KEY`/`GEMINI_MODEL`/`GEMINI_FALLBACK_ENABLED`) and policy-level docs (ADR 0004) existed. Research also found no Verifier report-detail page existed yet (`apps/web/src/app/verifier/` was 100% stub pages) and no DB table/RBAC entity for this feature.
**Decision:** **User-approved decision**: reconciled two conflicting mandated UI strings — CONTENT_GUIDE.md previously required "Saran AI (Gemini) — hanya referensi tambahan, bukan keputusan verifikator" for this exact concept; this block's own prompt specifies "Analisis Tambahan Eksternal — Tidak Menentukan Keputusan Resmi" verbatim. Adopted this block's string as canonical (exported as the single constant `GEMINI_ADVISORY_UI_LABEL` in `packages/domain/src/gemini-advisory.ts` so no call site can paraphrase it) and updated CONTENT_GUIDE.md to match, rather than leaving two competing mandated labels for the same UI element. **User-approved decision**: built a real (not stub) Verifier report-detail page at `/verifier/laporan/[reportId]` — the surface this feature's panel needs to attach to — showing the local ML model's prediction (still primary/authoritative) and the Gemini advisory panel below it, visually and structurally separated; the confirm/override/reject/request_info/escalate decision UI itself remains out of this block's scope (the underlying `submitVerificationDecision` service/route already exist from BLOCK 16, ready to be wired into this same page as a natural follow-up). **User-approved decision**: added `@google/genai` (Google's current, actively-maintained SDK with first-class `responseSchema` structured-output support) as a fresh `apps/web` dependency — no Gemini SDK existed in the repo before this block. **User-approved decision**: switched the write path from a plain RLS-gated `.insert()` to a `SECURITY DEFINER` RPC (`record_gemini_advisory_request`, mirroring `submit_verification_decision`'s exact shape: role check, `current_profile_id()` server-side actor resolution, insert, return the row) after research surfaced that every other Verifier-authored write in this codebase already uses this stronger pattern — never trusting a client-passed profile id.

Added a new RBAC entity, `gemini_advisory_request` (`RBAC_MATRIX.md`, `permissions.ts`'s `PERMISSION_TABLE`): Verifier gets `create`+`read`; Auditor gets `read`-only (so a prompt/response history can be reviewed after the fact per `THREAT_MODEL.md`'s Gemini Prompt Injection detection strategy); no other role has any access — verified by a cross-role-denial test extending the existing `permissions.test.ts` suite. New migration (`20260721090000_gemini_advisory.sql`) adds the `gemini_advisory_requests` table (status/image-disclosure-level/consent/disclosure/structured-output/error/model/latency/retry-count/cost columns, check constraints enforcing succeeded-implies-output-present and failed-implies-message-present) plus the RPC above — RLS grants only SELECT to Verifier/Auditor; there is no direct INSERT policy for any role, matching `verification_reviews`' own "immutable, RPC-only write path" precedent. Critically, this table and its RPC have zero code path that touches `reports.status` or `verification_reviews` — creating an advisory request is purely additive and informational, verified directly by a service-layer test asserting the only RPC ever called is `record_gemini_advisory_request`.

`apps/web/src/lib/gemini/` gained: `client.ts` (the sole Gemini call site — per-process sliding-window rate limiter, a hard timeout via `Promise.race`, bounded retries only on transient 429/5xx status codes, never on 4xx; structured-output-only via `responseSchema` + `responseMimeType: "application/json"`, re-validated against a Zod schema as defense-in-depth since a model could technically ignore the schema), `prompt.ts` (a fixed system instruction that never concatenates reporter-supplied text into instruction text — untrusted content is always wrapped in an explicitly labeled "UNTRUSTED REPORT CONTENT" data block, with the model explicitly told to treat any instruction-like text found inside it as suspicious data, not a command — directly implementing `THREAT_MODEL.md`'s "Gemini Prompt Injection" prevention strategy, which previously existed only as policy text with no code), `redaction.ts` (downscale-to-256px + blur + re-encode via `sharp`, already an `apps/web` dependency — the preferred default over sending a raw image), and `rate-limit.ts` (a minimal sliding-window limiter, mirroring `apps/ml-api/app/rate_limit.py`'s same shape, since no TypeScript-side rate limiter existed to reuse). `lib/reports/service/gemini-advisory.ts` orchestrates: fetch report/prediction/evidence, apply the requested disclosure level (download+redact only when needed — never for `imageDisclosureLevel: "none"`), call Gemini, and record every outcome (succeeded/failed/timed_out/rate_limited) as one complete audit row via the RPC — a failed call is not an unhandled exception the caller must separately log; it IS the audit trail. A defense-in-depth guard re-checks that `raw_image` disclosure requires both `consentAccepted` and `externalDisclosureAccepted` at the service-function level (in addition to the Zod schema's `z.literal(true)` at the route boundary and the RPC's own check), so the raw image is never even downloaded, let alone sent externally, without both acknowledgements true for that specific request.

`GeminiAdvisoryPanel.tsx` (Verifier-facing client component): a disclosure-level radio selector (none/redacted/raw), a consent+external-disclosure checkbox gate shown only when an image is selected, a request button disabled until both acknowledgements are checked (when applicable), and a rendered history of every prior advisory call (including failed ones) for the report — the request is only ever triggered by this explicit button click, never automatically on page load or via polling. When `GEMINI_API_KEY` is unset, the panel renders a disabled, explanatory state instead of the request form, and the rest of the Verifier workflow is entirely unaffected — verified directly via a live dev-server build and request with a blank `GEMINI_API_KEY`.

**Consequences:** Extended `packages/domain/src/env.ts`'s `serverEnvSchema` with `GEMINI_TIMEOUT_MS`/`GEMINI_MAX_RETRIES`/`GEMINI_RATE_LIMIT_REQUESTS_PER_MINUTE` (all defaulted, so zero additional configuration is required beyond the pre-existing `GEMINI_API_KEY`/`GEMINI_MODEL`), added corresponding `.env.example` entries. Extended `apps/web/src/lib/reports/service/test-support/fake-db.ts` with a minimal `.storage.from(bucket).download(path)` stub (mirroring the file's existing "only implements what's actually called" philosophy) so the new service-layer tests don't need a live Supabase Storage instance. Wrote 59 new tests: 26 in `lib/gemini/` (client — mocked `@google/genai` via a hoisted class-based mock, covering success/timeout/rate-limit/retry-vs-no-retry/invalid-response paths; prompt-injection defense; redaction via real `sharp` calls on synthetic images; rate limiter with fake timers), 16 in `lib/reports/service/gemini-advisory.test.ts` (precondition failures, all three image-disclosure levels, the "never touches reports.status/verification_reviews" guarantee, every outcome status being recorded, RPC error-code translation), 1 new cross-role-denial case in `permissions.test.ts`, and 16 in `packages/domain/src/gemini-advisory.test.ts` (request schema, structured-output schema bounding/rejecting malformed responses, response schema, the exact required label string). Confirmed via a full `next build` with a blank `GEMINI_API_KEY` that the app builds and serves correctly with zero Gemini configuration, and via a live dev-server request that both the new Verifier detail page and the Gemini advisory API route correctly redirect/deny when unauthenticated, matching every other role-gated route's existing behavior. Confirmed via `grep` over `.next/static/chunks/` that no actual Gemini API key value ever reaches a client bundle; found (but did not introduce, and left as a disclosed pre-existing condition out of this block's scope) that `serverEnvSchema`'s field-name strings (not secret values) for every server secret including `GEMINI_API_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, and `SESSION_SIGNING_SECRET` already reached one client chunk before this block — a schema-shape leak, not a secret-value leak, and not something this block's own code caused. `pnpm lint`/`typecheck`/`test`/`build` all pass across `apps/web` (190 tests) and `packages/domain` (46 tests), with no regression in any pre-existing test.
**Status:** Accepted

### 2026-07-22 — Verifier dashboard, queue, map, reports, detail, and human validation workflow (BLOCK 23)

**Context:** BLOCK 23 required a full Verifier surface: a Ringkasan dashboard (waiting/SLA-warning/low-quality/duplicate/high-severity counts, decisions today), an Antrean Verifikasi queue filterable by predicted severity/confidence/uncertainty/quality/duplicate/GPS accuracy/age/assignment, a multi-pin evidence map, a Semua Laporan cross-status list, a Permintaan Informasi list, and a report-detail screen showing private evidence with zoom, quality, duplicate links, GPS source/accuracy/boundary, probability bars, calibration/uncertainty, Grad-CAM, model metadata, the optional Gemini advisory panel, reporter text, and a full review-history timeline — plus six decision actions (Confirm, Override+reason, Reject+reason-category+note, Request Information, Insufficient Evidence, Escalate), all via transactions/audit events/immutable review history/a superseding review rather than destructive editing, with the Coordinator continuing to never see unverified private evidence by default. Research confirmed `insufficient_evidence`, `supersedes_review_id`, `reject_reason_category`, and `reports.escalated` did not exist anywhere before this block, and that the Coordinator's "must not see unverified private evidence" requirement was already fully satisfied by BLOCK 08's existing RLS policies (each independently re-checking `reports.status = 'verified'`) — no new RLS was needed for this block.
**Decision:** **User-approved decisions** (four `AskUserQuestion` rounds, all recommended options accepted): (1) `insufficient_evidence` is a new `verification_decision` enum value distinct from `request_info`, moving a report back to `needs_manual_review`; (2) superseding reviews are modeled via a new nullable `verification_reviews.supersedes_review_id` self-reference rather than any UPDATE/DELETE, preserving the "immutable review history" requirement literally; (3) escalation is tracked via a new `reports.escalated` boolean set by the RPC, rather than overloading `status`; (4) reject reasons are a new `verification_reject_reason` enum column (`insufficient_evidence`/`not_disaster_related`/`duplicate_report`/`fraudulent_or_spam`/`outside_event_boundary`/`other`), required exactly when `decision = 'reject'` (a check constraint enforces this both ways). Also user-approved: Peta Bukti is a real multi-pin MapLibre map (not a stub), and the SLA warning threshold is a documented-provisional 24 hours since submission (no real operational data exists yet to derive it from, matching BLOCK 19/20's own disclosed-provisional-threshold posture).

Split the schema change across two migrations because Postgres forbids using a newly-added enum value (`ALTER TYPE ... ADD VALUE`) in the same transaction that adds it: `20260722070000_verifier_review_schema.sql` adds the enum value, the new `verification_reject_reason` enum, `verification_reviews.reject_reason_category`/`supersedes_review_id`, their check constraints, and `reports.escalated`; `20260722070001_verifier_review_rpc.sql` then `drop function if exists` + recreates `submit_verification_decision` (a changed parameter list would otherwise create a second overload rather than replace the first) with the new decision handling, escalated-flag computation, and supersedes-chain population, all still inside one `SECURITY DEFINER` transaction with the existing role-check/`current_profile_id()`/row-lock/audit-event pattern. `20260722070002_verifier_queue_view.sql` adds `public.verifier_report_queue`, a `security_invoker = true` view joining each report to its LATEST `model_predictions`/`geolocation_observations`/`verification_reviews` row via `LEFT JOIN LATERAL ... ORDER BY ... LIMIT 1` (a plain `reports` query has no way to filter on "latest child row"), extracting top-severity/top-confidence from the `severity_probabilities` jsonb via `jsonb_each_text()` — chosen specifically because `security_invoker` re-checks RLS on the underlying tables as the calling role, adding zero new authorization surface, verified by inspection that every column exposed is already independently readable by a Verifier today.

`packages/domain`: added `insufficient_evidence` to `VERIFICATION_DECISIONS` and the new `VERIFICATION_REJECT_REASONS` enum (`enums.ts`); extended `reportListFiltersSchema` with nine new optional queue-filter fields; rewrote `submitVerificationDecisionSchema` with `rejectReasonCategory` and six `.refine()` rules (override/reject-conditional-field requirements, notes required for override/reject/escalate). `apps/web/src/lib/reports/service/`: `list.ts` gained `listQueueReports` (queries the new view, chains all nine filters, `minAgeHours` computed as a `submitted_at` cutoff); `dashboard.ts` (new) computes six dashboard metrics as parallel `{count:"exact", head:true}` queries against the queue view plus one against `verification_reviews`, with `SLA_WARNING_HOURS = 24` exported and documented as provisional; `evidence.ts` (new) lists a report's evidence with fresh per-request signed URLs (image + thumbnail), reusing the exact RLS-scoped-client + `createSignedUrl()` pattern BLOCK 15's signed-url route already established, omitting (not failing on) a row whose object can't be signed; `analysis.ts` (new) fetches the latest `model_predictions` row joined with `model_registry_entries` metadata and every `model_explanations` row, and computes a normalized-entropy `uncertainty` value client-side from the probabilities already returned — disclosed explicitly as NOT the ECE/calibration-curve metric BLOCK 20 computes offline in `ml/`, since no per-prediction calibration parameter is persisted anywhere in this schema; `review-history.ts` (new) lists the full immutable `verification_reviews` history including the `supersedes_review_id` chain; `information-requests.ts` (new) lists reports whose latest review is `request_info`, deliberately filtering the small result set in TypeScript after fetch rather than via a PostgREST embedded-resource dot-path filter (`.eq("reports.status", ...)`) — a real but fragile-to-rely-on feature with zero other precedent in this codebase — with the honest disclosed limitation that no "response received" tracking column exists; a report simply leaves the list once a Verifier acts on it again.

New/rewritten pages under `apps/web/src/app/verifier/`: `page.tsx` (Ringkasan — six `MetricCard`s linking to pre-filtered Antrean URLs), `antrean/page.tsx` + `QueueFilters.tsx`/`QueueList.tsx`, `laporan/page.tsx` (Semua Laporan, using the original unfiltered `listReports`) + `AllReportsList.tsx`, `peta/page.tsx` + `PetaBuktiFilters.tsx`/`EvidenceMap.tsx`/`EvidenceMapClient.tsx` (a new multi-pin MapLibre map, distinct from the existing single-pin `MapPin.tsx`, colored via `severityColors`, click-to-navigate), `permintaan-informasi/page.tsx` + `InformationRequestList.tsx`. Filter UI throughout uses the URL-search-params-as-state pattern (`useSearchParams()`/`useRouter().push()`, never local `useState`) so filtered views stay bookmentable/shareable and the server component simply re-queries on navigation. `laporan/[reportId]/page.tsx` (BLOCK 22's minimal version) was rewritten to compose: `EvidenceGallery` (new — a zoom `Dialog` over signed-URL thumbnails/originals), `ModelAnalysisPanel` (new — probability bars, quality/uncertainty meters, the duplicate-candidate link, the occlusion-sensitivity heatmap labeled by its real name rather than "Grad-CAM" per `apps/ml-api/app/explain.py`'s own disclosed technique-naming trade-off, and model registry metadata), `LocationTrustPanel` (BLOCK 17 — fully built but never mounted anywhere before this block), the existing `GeminiAdvisoryPanel` (unchanged), `ReviewHistoryTimeline` (new, using `packages/ui`'s existing `Timeline` primitive), and the new `DecisionPanel` (all six actions, one active at a time, each showing exactly the extra fields `submitVerificationDecisionSchema` requires for that decision) — rendered only while the report is in `analysis_completed`/`needs_manual_review`, matching what the RPC actually accepts.

`packages/ui/src/tokens/labels.ts` gained `insufficient_evidence` in `verificationDecisionLabels`, a new `verificationDecisionStatusLabels` (past-tense/status labels for the Timeline), and `rejectReasonLabels`, all mirroring `CONTENT_GUIDE.md`'s tables exactly.
**Consequences:** Extended `apps/web/src/lib/reports/service/test-support/fake-db.ts` with `.gte`/`.lte`/`.not` recorded methods (needed by `listQueueReports`/`getVerifierDashboardMetrics`) and a `storageSignedUrl` config surface (mirroring the existing `storage`/`.download()` stub) so `evidence.ts` is unit-testable without live Supabase Storage. Wrote new tests: `evidence.test.ts`, `analysis.test.ts` (including `computeUncertainty`'s near-0/near-1 boundary behavior), `review-history.test.ts`, `dashboard.test.ts`, and queue-filter/`listQueueReports` cases added to `list.test.ts`; `reports.test.ts` gained ~8 new cases for the queue filters and the new decision-schema refinement rules (and one pre-existing override-decision test was fixed to supply the now-required `notes` field). `docs/product/CONTENT_GUIDE.md` gained the `insufficient_evidence` decision-label row and the Reject Reason Categories table; `docs/product/STATE_MACHINES.md` gained the `insufficient_evidence` transition section. Disclosed, not fixed (out of this block's scope): Permintaan Informasi has no "response received" signal distinct from "still needs_manual_review with a request_info latest review"; per-prediction "calibration/uncertainty" is a client-computed entropy spread, not the offline ECE calibration metric BLOCK 20 produces, since none is persisted per-prediction. `pnpm lint`/`typecheck`/`test`/`build` all pass across `apps/web` (214 tests) and `packages/domain` (54 tests), with no regression in any pre-existing test; confirmed via `next build` that the production build (including the service-worker precache step) completes cleanly with every new route present.
**Status:** Accepted
